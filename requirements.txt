fastapi>=0.115
uvicorn[standard]>=0.34
apscheduler>=3.11,<4
pydantic>=2.9
pydantic-settings>=2.6
httpx>=0.27
jinja2>=3.1
mcp>=1.0  # client-only: loopback MCP client to Tower search_vault; NO model deps
pywebpush>=2.0    # Phase 2 will call this; installed now so the venv is ready
py-vapid>=1.9
pexpect>=4.9,<5  # bounded PTY capture of official Claude/Gemini /usage panels
